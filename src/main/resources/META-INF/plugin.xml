<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.asyncselfinvocationcompanion</id>
    <name>Async Self-Invocation Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a call to a Spring <code>@Async</code> method
        made from another method of the <b>same</b> declaring class --
        Spring's own documentation states this bypasses the AOP proxy
        entirely: "a call to the async-marked method from within the
        target object is not intercepted... self-invocation... effectively
        bypasses the proxy and its interceptor chain." The call still
        compiles and runs -- just synchronously, on the caller's thread,
        with no error and no warning. JetBrains Ultimate's Spring plugin
        already has this exact inspection for
        <code>@Transactional</code> (<code>SpringTransactionalMethodCallsInspection</code>)
        but not for <code>@Async</code> -- confirmed by inspecting the
        Spring plugin's own bundled <code>plugin.xml</code> before
        building this.</p>
        <p><b>100% static PSI analysis</b> -- matches <code>@Async</code> by
        simple annotation name, not by resolving the real Spring
        classpath symbol, so it works whether the real Spring jar is on
        the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags an unqualified or
        <code>this.</code>-qualified call within the exact same class --
        a call through an injected self-reference (the
        <code>@Lazy</code> proxy workaround) is correctly never
        flagged, since it's qualified by a different expression.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.asyncselfinvocationcompanion.gutter.AsyncSelfInvocationLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.asyncselfinvocationcompanion.gutter.AsyncSelfInvocationLineMarkerProvider"/>
        <notificationGroup id="Async Self-Invocation Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
